#include <stdio.h>
using namespace std;
int search(int i)
{
	for(int j=1;j<=8;j++)
	{
		if(Se puede colocar)
		{
			 Lugar reina
			 Posición de la marca
			if(i==8)  Salida
			else search(i+1);
			 Marca dónde colocar la reina, ¿puedes probar la siguiente posición?
		}
	}
}
